{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_bug_zapper",
            "type": "shell",
            "command": [
                "source ~/.devenv/humble.sh;",
                "cd ${workspaceFolder}/ros2;",
                "colcon build --packages-select bug_zapper",
                "--symlink-install",
                "--event-handlers console_cohesion+",
                "--cmake-args -DCMAKE_BUILD_TYPE=Debug"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}